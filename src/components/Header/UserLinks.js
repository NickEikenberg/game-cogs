import React from 'react';

const UserLinks = () => {
  return (
    <div className="space-x-2">
      <button>Login</button>
      <button>Register</button>
    </div>
  );
};

export default UserLinks;
